# Selected visual reference

`owner-selected-fallback-chibi.png` is the sole retained concept reference and the authoritative visual source for the version 0.1 mascot.

Five rejected tall references and derived transparency intermediates were removed from the working tree on 2026-07-28. Their decision history remains in `../../DesktopMascot.md`, and their binaries remain recoverable from Git history through commit `0f292ec`.

The frozen native sprite and its light/dark QA sheet live in `../production/`.
